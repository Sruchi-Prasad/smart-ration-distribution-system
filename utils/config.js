// Centralized configuration for the application
const IP_ADDRESS = "localhost"; // Use localhost for web, or your machine's IP for physical devices
export const API_BASE = `http://${IP_ADDRESS}:8000`;
